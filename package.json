{
  "name": "browser-sdk-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/browser-sdk && npm run build --workspace=packages/browser-sdk-mcp && npm run build --workspace=apps/site",
    "dev": "npm run dev --workspace=apps/site",
    "check": "npm run typecheck && npm test && npm run build",
    "test": "npm run test --workspace=packages/browser-sdk && npm run test --workspace=packages/browser-sdk-mcp",
    "typecheck": "npm run typecheck --workspace=packages/browser-sdk && npm run build --workspace=packages/browser-sdk && npm run typecheck --workspace=packages/browser-sdk-mcp && npm run typecheck --workspace=apps/site"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
